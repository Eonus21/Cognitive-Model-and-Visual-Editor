package fsm;

//import java.util.HashMap;

//Доделать Eventы, они пока не передается в Automat
//Сделать через ArrayList в Automat
class FSM {//Main
    public static void main ( String[] args) throws Exception{
        /*DataController dc = new DataController();

        HashMap<String, State> states= new HashMap<String, State>();
        states.put("default", new DefaultState());
        states.put("second", new SecondState());
        Transitions transitions = new Transitions();
        transitions.addTransition(new FirstTransition());
        transitions.addTransition(new SecondTransition());
        Automat a = new Automat(states, transitions.getHashMap(), 1000);
        Thread automatThread = new Thread(a);
        automatThread.start();*/
    }
}







